# ---------- DEV PROFILE ----------
pms.dummy = This is a dummy for dev environment

# DB URL and DB name
spring.datasource.url=jdbc:mysql://localhost:3306/pmsdb-dev?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver  # optional, Spring Boot can auto-detect

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Misc
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false
